````prompt
---
description: 'Generate project context'
---

# bmad-bmm-generate-project-context (Stub)

> **This is a stub.** Load and execute the full prompt from the release module.
> All `_bmad/` paths in the full prompt are relative to `bmad.lens.release/` — do NOT resolve paths against the user's main project repo.

```
Read and follow all instructions in: bmad.lens.release/.github/prompts/bmad-bmm-generate-project-context.prompt.md
```

````
